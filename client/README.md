# MERN Blog — Client

This is a Vite + React client for the MERN Blog assignment.

## Setup
1. Copy `.env.example` to `.env` and set `VITE_API_URL` if necessary.
2. Install:
   npm install
3. Run:
   npm run dev

The client proxies `/api` requests to `http://localhost:5000` (see vite.config.js).
